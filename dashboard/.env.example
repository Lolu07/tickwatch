# Copy to .env.local — Vite only exposes VITE_* vars to the browser
# Get the value from: terraform output api_url
VITE_API_URL=https://xxxxxxxxxx.execute-api.us-east-1.amazonaws.com

# Polling interval for the anomaly feed (milliseconds)
VITE_POLL_INTERVAL_MS=15000
