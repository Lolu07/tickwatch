# boto3 is pre-installed in the Lambda Python runtime — list it here only
# so the local venv and the deploy zip are reproducible.
boto3==1.34.131
botocore==1.34.131
# Phase 5: Claude API for plain-English anomaly explanations
anthropic>=0.50.0
